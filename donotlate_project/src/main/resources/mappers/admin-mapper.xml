<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.late.donot.admin.model.mapper.AdminMapper">

    <select id="login">
		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_NAME, MEMBER_PW, AUTHORITY,MEMBER_DEL_FL,
        PROFILE_IMG, ENROLL_DATE, UPDATE_DATE, SOCIAL_TYPE, SOCIAL_ID
		FROM "MEMBER"
		WHERE MEMBER_EMAIL = #{memberEmail}
		AND MEMBER_DEL_FL = 'N'
		AND AUTHORITY = '3'
	</select>
	
    <select id="getUsers">
		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_NAME, MEMBER_PW, AUTHORITY,MEMBER_DEL_FL,
        PROFILE_IMG, ENROLL_DATE, UPDATE_DATE, SOCIAL_TYPE, SOCIAL_ID
		FROM "MEMBER"
	</select>
	
	<update id="editUser">
		  UPDATE "MEMBER"
		  SET
		    MEMBER_NAME   = #{memberName},
		    MEMBER_EMAIL  = #{memberEmail},
		    AUTHORITY     = #{authority},
		    MEMBER_DEL_FL = #{memberDelFl}
		  WHERE MEMBER_NO = #{memberNo}
	</update>
	
	<delete id="removeUser">
		DELETE 
		FROM  MEMBER
		WHERE MEMBER_NO = #{memberNo}
	</delete>
	
	<insert id="createUser">
		INSERT INTO "MEMBER"
		(
		    MEMBER_NO,
		    MEMBER_EMAIL,
		    MEMBER_NAME,
		    MEMBER_PW,
		    AUTHORITY,
		    MEMBER_DEL_FL,
		    ENROLL_DATE
		)
		VALUES
		(
		    SEQ_MEMBER.NEXTVAL,
		    #{memberEmail},
		    #{memberName},
		    #{memberPw},
		    #{authority},
		    #{memberDelFl},
		    SYSTIMESTAMP
		)
	</insert>
	
	

	<select id="Notices">
	  SELECT
	    N.BOARD_NO,
	    N.BOARD_TITLE,
	    N.BOARD_CONTENT,
	    N.BOARD_WRITE_DATE,
	    N.BOARD_VIEW_COUNT,
	    N.BOARD_DEL_FL,
	    N.CATEGORY_NO AS categoryNo,
	    C.CATEGORY_NAME AS categoryName,
	    M.MEMBER_NAME,
	    N.THUMBNAIL_URL
	  FROM NOTICE N
	  JOIN MEMBER M ON N.MEMBER_NO = M.MEMBER_NO
	  JOIN NOTICE_CATEGORY C ON N.CATEGORY_NO = C.CATEGORY_NO
	  ORDER BY N.BOARD_WRITE_DATE DESC
	</select>


	<insert id="createBoard">
	    INSERT INTO NOTICE (
	        BOARD_NO,
	        BOARD_TITLE,
	        BOARD_CONTENT,
	        CATEGORY_NO,
	        MEMBER_NO,
	        THUMBNAIL_URL
	    ) VALUES (
	        SEQ_BOARD.NEXTVAL,
	        #{boardTitle},
	        #{boardContent},
	        #{categoryNo},
	        #{memberNo},
	        #{thumbnailUrl}
	    )
	</insert>
	
	
	<delete id="removeNotice">
		DELETE 
		FROM  NOTICE
		WHERE BOARD_NO = #{boardNo}
	</delete>
	
	
	<update id="editBoard">
		UPDATE NOTICE
		SET
			BOARD_TITLE = #{boardTitle},
			BOARD_CONTENT = #{boardContent},
			CATEGORY_NO = #{categoryNo},
			BOARD_DEL_FL = #{boardDelFl}
		<if test="thumbnailUrl != null and thumbnailUrl != ''">
  			, THUMBNAIL_URL = #{thumbnailUrl}
		</if>
		WHERE BOARD_NO = #{boardNo}
	</update>



</mapper>